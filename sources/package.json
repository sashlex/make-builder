{
  "name": "sources",
  "version": "1.0.0",
  "description": "sources",
  "main": "index.js",
  "scripts": {
     "postinstall": "cd ../dist && npm install",
     "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}
